import { toast } from "react-hot-toast";

export const handleRefund = () => {
  // Mock action
  toast.success("Refund successful!");
};

export const handleFreeze = () => {
  // Mock action
  toast.error("Transaction frozen!");
};
